class Example:
    """
    This class would have been imported from an external package that desn't have stubs,
    so the stubs are in `__init__.pyi`.
    """
    def __init__(self, a, b):
        self.a = a
        self.b = b
